# Nice-to-have / wishlist (SANDBOX)

- End-to-end differentiability (grad through the full coupled step) for calibration / sensitivity.
- `vmap` over scenario batches (lat/season/dilution-regime sweeps) in one call.
- Output trajectories to NetCDF/CSV alongside plots.
- A small web/notebook dashboard for switch toggling and result comparison.
- CI (GitHub Actions) running the test suites + a smoke coupled run on each PR.
- Two-way RF→temperature→dynamics beyond a box (column/1-D).
