# Python dependencies for the gas-phase chemistry box model.
# Pinned to the versions the model has been developed and validated against (Python 3.12).
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- Phase A (NumPy/SciPy port) + plotting + scenario configs ---
numpy==2.4.2
scipy==1.17.1
matplotlib==3.10.8
pyyaml==6.0.3

# --- Phase B (JAX implementation, src-python/jaxmodel/) ---
jax==0.9.2
diffrax==0.7.2
equinox==0.13.6
optax==0.2.8
optimistix==0.1.0   # used directly for the differentiable stiff-solve root finder
lineax==0.1.0       # ditto (AutoLinearSolver)

# --- tests ---
pytest
