.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
# NOTE: unlike the in-repo version, the bundled data/ and the reference NetCDF
# fixtures (tests/fixtures/*.nc) ARE committed here so the package is fully
# self-contained and self-validating on clone.
.DS_Store

# per-run text log (regenerated every run; the script always writes <out>/run.log)
run.log
coupled/analyses/
coupled/viz/cache/
# paper-ensemble run outputs: per-case state.npz + generated figures (regenerable via the
# runners and make_* scripts; see coupled/paper_ensemble/FIGURES.md)
coupled/paper_ensemble/runs*/
coupled/paper_ensemble/overnight_launch.log
# archived MATLAB snapshots kept locally in the leftover gas_phase_chemistry/ dir
# (the model itself moved to the stratchem-jax submodule)
gas_phase_chemistry/
